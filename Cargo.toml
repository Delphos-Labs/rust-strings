[package]
name = "rust-strings"
version = "0.7.0"
edition = "2021"
license = "MIT"
authors = ["Delphos Labs"]
description = "Streaming ASCII and Unicode UTF-16 string extraction for binary data"
readme = "README.md"
documentation = "https://docs.rs/rust-strings"
repository = "https://github.com/Delphos-Labs/rust-strings"
homepage = "https://github.com/Delphos-Labs/rust-strings"
keywords = ["strings", "encoding"]
categories = ["encoding"]
include = ["/LICENSE.txt", "/README.md", "/src", "/pyproject.toml"]

[lib]
name = "rust_strings"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "rust-strings"
path = "src/main.rs"
required-features = ["cli"]

[dependencies]
clap = { version = "4.5.8", features = ["derive"], optional = true }
pyo3 = { version = "0.21.2", features = ["extension-module", "gil-refs"], optional = true }

[dev-dependencies]
tempfile = "3.10"

[features]
python_bindings = ["pyo3"]
cli = ["clap"]
